# ETIC 验证清单（M1 – M6 全量）

本清单覆盖已完成里程碑 **M1–M6** 及卦象百科的全部可验证内容，供发版前逐条核对。
分为三类环境，按需执行：

| 类别 | 环境 | 说明 |
| --- | --- | --- |
| 🟢 引擎 | Linux / macOS | 纯 Swift，`swift test` 自动化，CI 覆盖 |
| 🟡 后端 | Linux / macOS（Python 3.12） | `pytest` 自动化；RAG 需 docker |
| 🔵 iOS | **仅 macOS + Xcode** | SwiftUI，手动验证（摇一摇/触觉需真机） |

勾选说明：`[ ]` 待验证 · `[x]` 通过 · 记录异常写入文末「问题记录」。

---

## 0. 构建冒烟

- [ ] 🟢 `cd Packages/DivinationEngine && swift build` 无错误、无警告
- [ ] 🟢 `swift test` 全部用例通过（23 项）
- [ ] 🟡 `cd Backend && pip install -r requirements-dev.txt && pytest` 全部通过（27 项）
- [ ] 🔵 `cd App && xcodegen generate && open ETIC.xcodeproj` 生成工程并在 iOS 17+ 模拟器上成功编译运行
- [ ] 🔵 App 冷启动进入「起卦」页，无崩溃、无控制台报错

---

## M1 — 确定性排盘引擎 🟢

自动化为主（`swift test`），可另行抽样人工核对经典卦例。

### 领域模型与静态数据
- [ ] 五行 / 阴阳 / 天干地支 / 六十甲子 / 八卦 / 64 卦 / 六亲 / 六神 / 旺衰 / 爻值 模型齐全
- [ ] 纳甲表、64 卦名 + 八宫世应表、二十四节气表（1900–2100）快照测试通过
- [ ] 静态表快照与基线一致（无意外改动）

### 历法换算
- [ ] 公历 → 干支四柱正确：立春换岁、节换月、五虎遁、五鼠遁、晚子时
- [ ] 20 个历法基准点与 `sxtwl` 比对一致

### 起卦
- [ ] 铜钱 / 数字 / 时间 / 随机 四种起卦均产出本卦 + 动爻 + 变卦
- [ ] 注入固定随机源时结果可复现（同输入恒同盘）
- [ ] 6 万次铜钱模拟老阳/少阴/少阳/老阴分布接近 1:3:3:1

### 排盘九步流水线
- [ ] 纳甲 → 世应 → 六亲 → 六神 → 动/变爻 → 旬空 → 旺衰 → 用神 全链路正确
- [ ] 经典卦例端到端核对（如「乾为天」六爻纳甲、世应、六亲、六神）
- [ ] 有动爻时变卦正确生成，动爻标记（○ 老阳 / × 老阴）正确

### 数据契约
- [ ] `DivinationBoard`（schema **v1.0.0**）字段完整、语义稳定；后端 Pydantic 模型与之一致

---

## M2 — iOS 起卦 + 静态排盘 UI 🔵

> 详见 `docs/TESTING-M2-M3.md`（逐步骤版）。以下为要点汇总。

### 起卦页
- [ ] 起卦方法选择（铜钱/数字/时间/随机）切换正常，方法说明随之更新
- [ ] 「所问何事」多行输入正常
- [ ] 9 类事项类别可选，单选高亮正确
- [ ] 起卦时间 DatePicker 可选
- [ ] 「起卦」按钮触发 → 进入排盘（或动画）流程
- [ ] 底部免责声明可见

### 排盘页
- [ ] 卦名、上下卦、卦宫显示正确
- [ ] 有动爻时可切换「本卦 / 变卦」
- [ ] 盘面表逐行显示：六神 · 六亲 · 干支 · 爻象 · 世/应 · 旬空 · 旺衰
- [ ] 爻象笔画正确（阳整笔 / 阴断笔 + ○× 动爻标记）
- [ ] 四柱（年月日时）+ 旬空显示正确
- [ ] 用神建议显示
- [ ] UI 严格只读消费引擎盘面，无任何术数计算下放到客户端

---

## M3 — 占卜动画（水墨成卦 / 摇一摇 / 触觉） 🔵

> 详见 `docs/TESTING-M2-M3.md`。摇一摇与触觉**仅真机可测**。

- [ ] 起卦后播放阶段动画：罗盘入场 → 摇卦 → 成卦 → （有动爻）动爻变卦 → 盘面浮现
- [ ] 三枚铜钱翻滚落定，落定结果与最终爻象**必然一致**（背数反推与引擎一致）
- [ ] 六爻自下而上水墨笔触「写出」（阳连笔 / 阴断笔）
- [ ] 动爻闪烁标记，本卦 → 变卦形变过渡顺滑
- [ ] 排盘信息逐项淡入上浮
- [ ] 「跳过动画」直达盘面
- [ ] 系统「减弱动态」(Reduce Motion) 开启时自动降级/直达盘面
- [ ] 🔵真机：摇一摇（CoreMotion）可触发摇卦
- [ ] 🔵真机：触觉反馈（CoreHaptics，降级 UIImpactFeedback）随铜钱落定
- [ ] 设置页「跳过动画 / 摇一摇 / 触觉」开关持久化生效

---

## M4 — LLM 解读 + 多轮对话 🟡🔵

### 后端 🟡
- [ ] `uvicorn app.main:app --port 8000` 启动；`GET /healthz` 正常
- [ ] 无 key 时自动 mock，返回桩解读，无需真实 key
- [ ] `POST /v1/interpret` SSE 流式返回（`data:{"delta":...}` → `[DONE]`）
- [ ] `POST /v1/chat` 多轮追问基于同一盘面，不重新起卦
- [ ] System Prompt 硬约束：只解读不改盘；吉凶引用具体爻位/六亲/六神/旺衰/旬空
- [ ] 客户端不持有 key、不直连模型（一律走后端）
- [ ] `pytest` 中 prompt 组装 + SSE 接口用例通过

### iOS 🔵
- [ ] 排盘页「请大师解读」入口可进入解读页
- [ ] 流式打字气泡逐字显示，「凝神推演…」loading 正常
- [ ] 追问输入框可发送后续问题，多轮回复携带同一盘面上下文
- [ ] 断网/后端未起时有合理错误提示，不崩溃
- [ ] （可选）配置真实 LLM key 后解读内容合理、援引盘面要素

---

## M5 — RAG 卦爻辞 grounding + 经文参考展示 🟡🔵

### 后端 RAG 🟡
- [ ] `docker compose up -d` 起 Postgres + pgvector
- [ ] `python scripts/ingest.py` 灌库成功（64 卦 → 448 文档）
- [ ] 无 embeddings key 时走确定性 mock 向量，灌库/检索/测试可离线完成
- [ ] `ETIC_RAG_ENABLED=true` 后 `/v1/interpret`、`/v1/chat` 检索「本卦卦辞 + 动爻爻辞 + 变卦卦辞」拼入 Prompt
- [ ] `rag_enabled=false` 或 DB 不可达时静默退化为 M4 行为（不报错、不阻塞）
- [ ] `POST /v1/grounding` 返回结构化经文项（出处/卦名/文档类型/爻位/内容）
- [ ] RAG 关闭时 `/v1/grounding` 返回空、不影响解读
- [ ] `pytest` 27 项通过（含 pgvector 实跑集成，无库时自动 skip）

### iOS 经文参考展示 🔵
- [ ] 解读页顶部「经文参考」可折叠卡片，进入页面时与首轮解读并行拉取一次
- [ ] 展示本卦卦辞 / 动爻爻辞 / 变卦卦辞，出处 + 原文，文本可选中复制
- [ ] 未开 RAG 时卡片静默留空，不影响解读

---

## M6 — 本地历史 / 收藏（SwiftData） 🔵

> 依赖 **iOS 17+**（SwiftData）。见 PR #10。

- [ ] 起卦后自动在本地登记一条卦例（按盘面内容派生稳定主键去重）
- [ ] 解读产生的对话在流结束/离开页面时回写到同一条卦例
- [ ] 起卦页左上角「时钟」入口进入历史列表
- [ ] 历史列表按时间倒序；可按「收藏 / 事项类别」筛选
- [ ] 左滑收藏 / 右滑删除，收藏星标显示正确
- [ ] 详情页回看盘面快照 + 已存解读对话；「查看完整盘面」跳转
- [ ] 详情页「继续追问」进入解读页，基于同一盘面追问
- [ ] 无历史时显示空状态
- [ ] 数据仅存本地设备，不上云

---

## 卦象百科（64 卦经文浏览） 🔵

> 纯客户端、离线、只读。见 PR #11。

- [ ] 起卦页左上角「书」入口进入卦象百科
- [ ] 64 卦网格完整显示（短名 + 全名）
- [ ] 搜索框按卦名 / 短名 / 卦辞过滤（如「乾」「履」「贞」）
- [ ] 点击进入详情：卦辞 / 彖辞（可空） / 六爻辞（初→上）显示正确
- [ ] 经文可选中复制
- [ ] 完全离线可用（关闭网络仍可浏览）

---

## 分层合规回归（每次发版必检）

- [ ] 引擎无 SwiftUI / UIKit / 网络 / LLM 依赖
- [ ] 客户端不做任何术数计算，只读消费 `DivinationBoard`
- [ ] LLM 只解读不改盘、不参与计算
- [ ] LLM key 只经后端 `.env`，未写入前端代码 / 未提交进 git
- [ ] `DivinationBoard` schema 版本与引擎/后端/fixture 一致

---

## 问题记录

| 编号 | 里程碑 | 现象 | 期望 | 状态 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
